# React + Vite + ReactFlow Example

This code example uses React, Vite and ReactFlow to create a simple flowchart.

To install the dependencies run:

```bash
npm install
```

To start the development server run:

```bash
npm run dev
```
